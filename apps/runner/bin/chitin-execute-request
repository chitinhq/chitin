#!/usr/bin/env bash
# chitin-execute-request — bin wrapper invoking src/execute-request.ts via tsx.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec pnpm exec tsx "$SCRIPT_DIR/../src/execute-request.ts" "$@"
