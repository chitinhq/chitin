---
description: "Task list — 115 spec PR review gate"
---

- [ ] T001 [P] [US1] Implement the spec-PR discriminator at `go/orchestrator/cmd/chitin-orchestrator/spec_pr_classify.go` — extend the factory-listen webhook eligibility (spec 113 FR-001 surface) with a new helper `isSpecPR(prNumber int) bool` that calls `gh api repos/<owner>/<repo>/pulls/<N>/files` and returns true iff every `.filename` is matched by `^\.specify/specs/\d+-.*/`
- [ ] T002 [P] [US2] Implement `chitin-orchestrator spec-lint <spec-dir>` subcommand at `go/orchestrator/cmd/chitin-orchestrator/spec_lint.go` — reads `spec.md` + `tasks.md`, runs the 7 rules L01-L07 (FR-003), emits structured JSON `[{rule, file, line, severity, message}]` on stdout; named exit codes (0=clean, 2=warnings, 3=errors)
- [ ] T003 [P] [US2] Implement rule L01 (frontmatter complete) in `go/orchestrator/internal/speclint/l01_frontmatter.go` — YAML-parse the frontmatter, assert spec_id/title/status/owner/created/depends_on/related are all present + well-formed
- [ ] T004 [P] [US2] Implement rule L02 (cross-spec refs resolve) in `go/orchestrator/internal/speclint/l02_cross_refs.go` — for each id in `depends_on:` / `related:`, glob `.specify/specs/<id>-*` and assert exactly one match
- [ ] T005 [P] [US2] Implement rule L03 (task-to-FR coverage) in `go/orchestrator/internal/speclint/l03_task_fr.go` — regex-extract `FR-NNN` from tasks.md; regex-extract `**FR-NNN**` from spec.md; assert bidirectional cover (every task FR exists; every spec FR has at least one task)
- [ ] T006 [P] [US2] Implement rule L04 (event taxonomy closure) in `go/orchestrator/internal/speclint/l04_events.go` — find the canonical FR-NNN telemetry block (heuristic: FR with `Chain events` in its body), build the closed set, then assert every `event_type` (matching `[a-z_]+`) referenced ANYWHERE else in spec.md or tasks.md is in that set
- [x] T007 [P] [US2] Implement rule L05 (CLI surface check) in `go/orchestrator/internal/speclint/l05_cli.go` — regex-match every `gh api <path>` and assert path starts with `repos/`; regex-match every `chitin-orchestrator <sub>` and `chitin-kernel <sub>` and assert the subcommand is in `.specify/known-cli-surfaces.txt` OR is being introduced by this spec (heuristic: appears in an `FR-NNN` body that names it as a new subcommand)
- [ ] T008 [P] [US2] Implement rule L06 (reason taxonomy alignment) in `go/orchestrator/internal/speclint/l06_reason.go` — find canonical `reason:` value sets declared in an FR-NNN of this spec or its depends_on; assert every other `reason: "<x>"` referenced in spec.md or tasks.md matches one of those values
- [ ] T009 [P] [US2] Implement rule L07 (user-story test presence) in `go/orchestrator/internal/speclint/l07_us_test.go` — find each `### USN` header; assert an `**Independent test:**` paragraph appears within its section
- [ ] T010 [US2] Implement `PostLintViolations` activity at `go/orchestrator/activities/post_lint_violations.go` — POST one PR review comment per error-severity violation via `gh api repos/<owner>/<repo>/pulls/<N>/reviews -f event=COMMENT -F 'comments=[{...}]'`; dedup by (rule, file, line) by checking existing chitin-authored review comments before posting
- [ ] T011 [US1] Implement `SpecIterationWorkflow` at `go/orchestrator/workflows/spec_iteration.go` — mirrors `PRIterationWorkflow` (spec 113 T004) shape; selects a driver with capability `spec-author` (claudecode/codex with the spec-tuned prompt); reuses spec 112 US2's `worktree.Manager.Checkout`
- [ ] T012 [US1] Implement `BuildSpecIterationPrompt` pure helper at `go/orchestrator/workflows/spec_iteration_prompt.go` — template includes full current spec.md + tasks.md (not diff), the linter's violations as structured input distinct from Copilot comments, and the required output envelope per FR-006 (driver MUST address each lint violation by either fixing spec OR justifying allowlist patch)
- [ ] T013 [US3] Implement `ClassifyDesignJudgement` pure helper at `go/orchestrator/internal/speclint/judgement.go` — regex-set match per FR-007; returns `Mechanical | DesignJudgement` per comment. Operator-editable regex set at `.specify/judgement-phrases.txt`
- [ ] T014 [US3] Wire classifier into `SpecIterationWorkflow` — partition incoming Copilot review comments by classification; iterate Mechanical ones via the driver; emit `spec_iteration_escalated { reason: "design_judgement_required" }` for the DesignJudgement set; both partitions can fire in the same round
- [ ] T015 [US1] Implement `dispatchSpecIteration` at `go/orchestrator/cmd/chitin-orchestrator/spec_iteration_dispatch.go` — given an eligible review payload on a spec-PR (FR-001), runs `spec-lint` then starts `SpecIterationWorkflow` with deterministic WorkflowID `spec-iteration-pr-<N>-review-<M>`; injectable temporal dialer + nil-stderr guard per spec 112 US2 pattern
- [ ] T016 [US1] Implement `EmitSpecIterationTelemetry` activity — emits all 6 chain events from FR-009 via the kernel-emit shell-out pattern; honors `CHITIN_DISABLE_CHAIN_EMIT=1`
- [ ] T017 [US1] Implement spec 114 FR-008 reason-taxonomy extension — add the `design_judgement_required` and `lint_violation_unresolvable` kinds so spec-PR escalations surface in the same queue; small change to `internal/queue/filter.go` to map `spec_iteration_escalated` events into the queue
- [ ] T018 [P] [US2] Create the initial `.specify/known-cli-surfaces.txt` allowlist — extract the current state of `chitin-orchestrator` + `chitin-kernel` subcommands (run each with no args, parse the help shape) and commit the file as the linter's starting allowlist
- [ ] T019 [P] [US3] Create the initial `.specify/judgement-phrases.txt` allowlist with a hand-curated set: "consider", "might want", "is this really", "could be", "should this be split", "should this be merged", "P1|P2|P3 priority", "in scope", "out of scope"
- [ ] T020 [US2] Hermetic test in `internal/speclint/lint_test.go` — golden fixture spec dirs covering each rule's pass + fail cases; assert structured JSON output shape + exit codes
- [ ] T021 [US1] Hermetic test in `workflows/spec_iteration_test.go` — Temporal testsuite env, stub driver returns a known fixup + lint allowlist patch, assert workflow result has correct action_counts.lint_fix + emits all expected chain events
- [ ] T022 [US1] HTTP-route test in `factory_listen_pr_test.go` — spec-class PR triggers `SpecIterationWorkflow`; code-class PR triggers `PRIterationWorkflow` (113); mixed-class PR routes to `PRIterationWorkflow` only (per spec 115 edge case)
- [ ] T023 [US3] Hermetic test in `speclint/judgement_test.go` — 20 hand-labelled Copilot comments (10 mechanical, 10 judgement); assert classifier hits ≥ 80% precision on judgement (SC-003) and ≥ 90% recall on mechanical
- [ ] T024 [US1] Operator runbook at `docs/runbooks/spec-115-spec-review-gate.md` — example `spec-lint` invocations, how to refresh the allowlist, how to silence a rule per spec, how to force-iterate a misclassified judgement comment
- [ ] T025 [US2] After spec 115 deploys: measure SC-002 by replaying the last 10 spec PRs' Copilot review comments through the linter; document the catch rate in the runbook
