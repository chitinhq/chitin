---
description: "Task list — 123 auto-merge on chitin/ready-to-merge label"
---

- [ ] T001 [US1] Define the `AutoMergeWorkflow` skeleton at `go/orchestrator/workflows/auto_merge.go` per FR-001. Input/Output types: `AutoMergeInput{Repo, PRNumber, LabelName, TriggerEventID}` + `AutoMergeResult{Outcome, MergeSHA, FailureReason}`. The workflow is deterministic — every external call goes through an activity per spec 070's purity contract. Register the workflow in the same place as `PRIterationWorkflow`
- [ ] T002 [US1] Implement the `CheckPRMergeability` deterministic activity at `go/orchestrator/activities/auto_merge_check.go` per FR-004 + FR-003. Shells out to `gh pr view <N> --json statusCheckRollup,mergeStateStatus,mergeable,isDraft,state,labels` and returns a typed `MergeabilityReport{CIStatus, IsMergeable, IsDraft, IsOpen, HasLabel, FailedChecks}`. Maps the gh JSON to the closed taxonomy in FR-004 (`green` / `failed` / `pending`). `HasLabel` per FR-003 — the workflow uses this to re-validate the label is still present at the start of each cycle (the label could have been removed between webhook receipt and workflow start)
- [ ] T003 [US1] Implement the `MergePR` deterministic activity at `go/orchestrator/activities/auto_merge_act.go` per FR-008. Shells out to `gh pr merge <N> --squash --delete-branch`. Returns `MergeResult{Succeeded, MergeSHA, HeadBranchDeleted, StderrTail}`. `StderrTail` is capped at 1 KiB. Parses the merge SHA from `gh`'s stdout
- [ ] T004 [US3] Implement the `UnlabelPR` deterministic activity at `go/orchestrator/activities/auto_merge_act.go` per FR-007. Shells out to `gh pr edit <N> --remove-label "chitin/ready-to-merge"`. Tolerates "label not found" stderr (concurrent operator action) — returns success with `was_present: false` instead of error
- [ ] T005 [US3] Implement the `CommentPR` deterministic activity at `go/orchestrator/activities/auto_merge_act.go` per FR-006. Input: PR number + template id (`ci_failed` | `merge_conflict` | `ci_timeout`) + template params. Renders the named template from a fixed constant set; shells out to `gh pr comment <N> --body <rendered>`. Dedup per the FR-014 / edge-case requirement: query the chain for prior `auto_merge_*` events with the same workflow_id and skip if the same template already fired
- [ ] T006 [US2] Implement the CI-pending wait loop in the workflow per FR-005. Uses `workflow.Sleep` with backoff `60s, 120s, 240s, 480s, 480s...` capped at `merge_timeout_seconds` (default 3600). Each tick emits one `auto_merge_waiting` event with `elapsed_seconds` and `ci_state`. Total elapsed > timeout → return to FR-005's timeout branch (T010)
- [ ] T007 [US1] Wire `pull_request.labeled` event dispatch into `handlePR` at `cmd/chitin-orchestrator/factory_listen.go` per FR-002. When `X-GitHub-Event` is `pull_request` AND `action == "labeled"` AND `label.name == activities.ReadyToMergeLabel`, start `AutoMergeWorkflow` with deterministic `WorkflowID` per FR-009. Existing event handlers MUST be unaffected — table-driven dispatch within `handlePR`
- [ ] T008 [US7] Set `WorkflowIDReusePolicy` to `RejectDuplicate` on the workflow start call per FR-009. On Temporal returning the "workflow already exists" error, the webhook handler logs + emits `auto_merge_already_running` (with the conflicting workflow id) and returns 200 to GitHub — duplicates are NOT errors at the HTTP layer
- [ ] T009 [US1] Emit the closed-taxonomy chain events from the workflow per FR-011. Ten event types total; payloads match the data-model section of spec.md exactly. The emit pattern follows spec 121's `KernelEventSink` shape (shell out to `chitin-kernel emit` with a tmp envelope file, honor `CHITIN_DISABLE_CHAIN_EMIT=1`)
- [ ] T010 [US3] Implement the failure-path routing per FR-010 — three new closed-taxonomy reason values for spec 114: `auto_merge_ci_failed`, `auto_merge_conflict`, `auto_merge_ci_timeout`. Each failure path runs `UnlabelPR` (T004) + `CommentPR` (T005) + `Notify` (spec 114's existing activity) in sequence within the workflow. The change to spec 114's reason enum is the only cross-spec edit
- [ ] T011 [US6] Wire `pull_request.unlabeled` event dispatch into `handlePR` per US6 + FR-015. When the unlabeled event removes `chitin/ready-to-merge` from a PR whose `AutoMergeWorkflow` is currently running, send a Temporal signal `LabelRemoved`. The workflow's wait loop (T006) MUST listen for this signal via `workflow.Selector` — when fired, the workflow exits via `auto_merge_canceled` with reason `label_removed_mid_wait`. The other three canonical canceled-reasons declared by FR-015 (`label_removed_pre_flight`, `pr_closed_pre_flight`, `pr_is_draft`) are emitted from the pre-flight check in T002 / the workflow start in T001
- [ ] T012 [US1] Implement the FR-014 break-glass `CHITIN_AUTO_MERGE_DISABLED=1` env check at the top of the `handlePR` label-dispatch branch (T007). When set, log the delivery_id + return 200 without starting a workflow. No chain emit per the FR
- [ ] T013 [US1] Register the `AutoMergeWorkflow` type with the orchestrator worker at `cmd/chitin-orchestrator/main.go` per FR-012. One-line registration next to the existing workflow type registrations, plus a boot log line of the form `auto_merge_workflow=registered` (analogous to spec 121's `blob_inline_threshold=...` line) so operators can confirm at a glance that the workflow is loaded
- [ ] T014 [US1] Implement the `chitin-orchestrator auto-merge status <PR>` subcommand at `go/orchestrator/cmd/chitin-orchestrator/auto_merge.go` per FR-013. Reads the chain for `auto_merge_*` events filtered by `pr_number`, renders a fixed-column table to stdout. Exit codes per FR-013: 0 succeeded, 2 failed, 3 no events
- [ ] T015 [P] [US1] Hermetic test in `workflows/auto_merge_test.go` using the Temporal testsuite — stage a workflow with a stub `CheckPRMergeability` returning `green + mergeable + has_label + open + not_draft`, assert the workflow completes via `MergePR` activity within zero wait time, emits exactly `auto_merge_triggered` + `auto_merge_succeeded` (no `auto_merge_waiting`)
- [ ] T016 [P] [US2] Hermetic test for the CI-pending wait loop — stub returns `pending` for two cycles then `green`, assert workflow emits two `auto_merge_waiting` events with monotonically increasing `elapsed_seconds`, then completes via `MergePR`
- [ ] T017 [P] [US3] Hermetic test for CI-failed path — stub returns `failed` with `failed_checks: ["test", "Analyze (go)"]`, assert workflow emits `auto_merge_ci_failed` + runs `UnlabelPR` + `CommentPR` (template `ci_failed`) + escalates via `Notify` with reason `auto_merge_ci_failed`. No `MergePR` call
- [ ] T018 [P] [US4] Hermetic test for merge-conflict path — stub returns `not mergeable, mergeStateStatus: "DIRTY"`. Assert workflow emits `auto_merge_conflict` + runs `UnlabelPR` + `CommentPR` (template `merge_conflict`) + escalates with reason `auto_merge_conflict`
- [ ] T019 [P] [US5] Hermetic test for CI-timeout path — stub returns `pending` indefinitely. Use the testsuite's deterministic clock to advance past `merge_timeout_seconds`. Assert exactly one `auto_merge_ci_timeout` event, one unlabel, one comment with template `ci_timeout`, one escalate
- [ ] T020 [P] [US6] Hermetic test for label-removed-mid-wait — start workflow with `pending` CI, fire the `LabelRemoved` signal mid-sleep. Assert workflow emits `auto_merge_canceled` with reason `label_removed_mid_wait` and exits without merging, unlabeling (already gone), commenting, or escalating
- [ ] T021 [P] [US7] Hermetic test for duplicate `labeled` events — fire two starts with the same `WorkflowID` within 1s. Assert: first one runs to completion (`auto_merge_succeeded`), second one rejects with `auto_merge_already_running` event + the first workflow's id in the `conflicting_workflow_id` field. After the first completes, a THIRD start (re-labeled, e.g.) succeeds — proves the reuse policy doesn't block legitimate retries (SC-005)
- [ ] T022 [P] [US1] HTTP-route test in `cmd/chitin-orchestrator/factory_listen_pr_test.go` — POST a synthetic `pull_request.labeled` payload with the `chitin/ready-to-merge` label name, assert the handler dispatches to a stub workflow starter with the right `AutoMergeInput`. Same fixture with a DIFFERENT label name asserts no dispatch. Same fixture with `action: "unlabeled"` asserts the signal path (T011) is taken, not the start path
- [ ] T023 [P] [US1] Break-glass test for FR-014 — `CHITIN_AUTO_MERGE_DISABLED=1` set, fire a `labeled` webhook, assert the handler returns 200 and emits no chain events and starts no workflow. Unset, fire the same webhook, assert normal dispatch
- [ ] T024 [US1] Extend spec 114's reason taxonomy with the three new closed values per FR-010 + T010. The change touches `internal/queue/filter.go` (the reason enum). Keep it minimal — three new closed values, no behavior change to existing kinds. A regression test in `internal/queue/filter_test.go` covers each new value
- [ ] T025 [US1] Operator runbook `docs/runbooks/spec-123-auto-merge.md` — what triggers auto-merge, what the three failure modes look like, how to read the chain events with `chitin-orchestrator auto-merge status`, how to invoke the break-glass `CHITIN_AUTO_MERGE_DISABLED=1`, what to do when a PR is stuck (re-apply the label, check CI), how this composes with spec 112's sibling-rebase. Cross-link to spec 116 (the producer) and spec 114 (the escalation surface) and the 2026-05-26 PR #1135 evidence
- [ ] T026 [US1] Replay validation: after deployment, observe the next PR to receive `chitin/ready-to-merge` from spec 116. Assert (a) `auto_merge_triggered` event fires within 60s; (b) terminal event is `auto_merge_succeeded` if CI is green; (c) PR `state` becomes `MERGED`; (d) operator did NOT click merge. This is the SC-001 proof and the canonical end-to-end demonstration of US1
