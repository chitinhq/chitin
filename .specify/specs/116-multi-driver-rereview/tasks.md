---
description: "Task list — 116 multi-driver iteration re-review"
---

- [ ] T001 [P] [US1] Implement the reviewer-pool resolver at `go/orchestrator/activities/internal_rereview_pool.go` — read the `CHITIN_INTERNAL_REREVIEW_POOL` env, intersect against the driver registry, apply spec 094 R-AUTHORID exclusion against the fixup author id, return the selected driver id (or empty + reason if no eligible driver remains)
- [ ] T002 [P] [US1] Implement the `DispatchInternalReview` activity at `go/orchestrator/activities/internal_rereview.go` — fetches fixup-commit diff via `git show`, fetches the ORIGINAL Copilot review body + comments via `gh api repos/<o>/<r>/pulls/<N>/reviews/<M>` and `.../comments`, builds the re-review prompt, invokes the selected driver, parses StructuredVerdict per spec 094
- [ ] T003 [P] [US1] Implement `BuildInternalReviewPrompt` pure helper at `go/orchestrator/activities/internal_rereview_prompt.go` — template that injects fixup diff + original review context + PR description + required StructuredVerdict envelope. Pure function; hermetic-testable
- [ ] T004 [US1] Implement `PostStructuredReview` activity at `go/orchestrator/activities/post_structured_review.go` — POST the verdict via `gh pr review <PRNumber> --comment --body <human-summary + fenced JSON>` (NOT `--approve`; only operator clicks approve). Body format MUST include the canonical StructuredVerdict JSON in a fenced block so subsequent tools can parse it
- [ ] T005 [US1] Implement extended StructuredVerdict schema in `go/orchestrator/activities/review/verdict/verdict.go` — add the `confidence: "high" | "medium" | "low"` field; default to `medium` on omit for backward compatibility; update `verdict.Validate` to accept the new field
- [ ] T006 [US1] Implement the spec 113 iteration-workflow extension at `go/orchestrator/workflows/pr_iteration.go` — on the `PushedFixup: true` path, sequentially call `DispatchInternalReview` then `PostStructuredReview`; carry the verdict + confidence into the result
- [ ] T007 [US2] Implement the `ready-to-merge` label application — when verdict is Approve or ApproveWithComments-with-empty-blockers, call `gh pr edit <N> --add-label ready-to-merge` from a new `ApplyReadyToMergeLabel` activity in `go/orchestrator/activities/apply_label.go`
- [ ] T008 [US3] Implement the low-confidence escalation — when verdict is Approve AND confidence is "low", apply BOTH `ready-to-merge` AND `chitin-escalated/low-confidence` labels; emit `internal_rereview_low_confidence` reason via the spec 114 escalation surface
- [ ] T009 [US1] Implement spec 113's `copilotReviewerLogins` extension — add `chitin-orchestrator` (the bot identity that posts internal reviews) to the allowlist so the internal-review webhook re-fires spec 113's iteration dispatcher for RequestChanges verdicts
- [ ] T010 [US1] Implement `EmitInternalRereviewTelemetry` activity — emits the 3 chain events from FR-011 via the kernel-emit shell-out pattern from spec 112 US2's `emitSiblingRebaseEvent`; honors `CHITIN_DISABLE_CHAIN_EMIT=1`
- [ ] T011 [US1] Implement spec 114 FR-008 reason taxonomy extension — add the `internal_rereview_low_confidence` reason kind so the queue surfaces the low-confidence-approve case under the right reason
- [ ] T012 [US1] Add hermetic test in `internal_rereview_pool_test.go` — assert R-AUTHORID exclusion works, empty-pool fallback fires, env-var precedence is correct
- [ ] T013 [US1] Add hermetic test in `internal_rereview_test.go` — stub driver returns a known StructuredVerdict; assert the verdict is parsed correctly + posted via gh stub (using the fake-gh pattern from spec 113 tests)
- [ ] T014 [US1] Add a unit test in `build_internal_review_prompt_test.go` — pure-function test; assert the prompt template includes fixup diff, original comments, and the required envelope shape
- [ ] T015 [US2] Add an integration test in `apply_label_test.go` — fake gh + driver returning Approve verdict; assert `ready-to-merge` label is applied + emit fires
- [ ] T016 [US3] Add a regression test for low-confidence escalation — driver returns Approve+confidence=low; assert BOTH labels applied + escalation event emitted
- [ ] T017 [US1] Implement the operator-host config example — add `CHITIN_INTERNAL_REREVIEW_POOL=codex,claudecode-haiku` to the example systemd unit dropin at `~/.config/systemd/user/chitin-orchestrator.service.d/`; document in the runbook
- [ ] T018 [US1] Operator runbook at `docs/runbooks/spec-116-multi-driver-rereview.md` — example invocations, how to configure the reviewer pool, how to interpret confidence levels, how to override a misclassified verdict
- [ ] T019 [US1] Add an integration test for the full round-2 flow — drive spec 113's loop end-to-end on a fixture PR with a stub Copilot review; assert that after fixup push, internal re-review fires, verdict is posted as a PR review, and the next iteration round triggers correctly via the webhook
