---
description: "Task list — 126 one GitHub issue per spec for operator visibility"
---

- [x] T001 [US1] Implement the `EnsureSpecIssue` deterministic activity at `go/orchestrator/activities/spec_issue.go` per FR-001 + FR-009. Input `{repo, spec_ref, spec_title, spec_pr_url, spec_md_url, tasks_md_url}`. First calls `gh issue list --label chitin/spec --search "<spec_ref>"` to check existence; if found, returns the existing number with `was_new: false`. If not, calls `gh issue create --label chitin/spec --title "[<spec_ref>] <title>" --body <rendered template>` and returns the new number with `was_new: true`. All `gh issue *` shell-outs MUST honor the `CHITIN_KERNEL_BIN` interception pattern per FR-009 — sibling activities (T002-T004) inherit the same convention. Idempotent on retry per the FR-007 contract
- [x] T002 [US2 US3 US5] Implement the `CommentSpecIssue` deterministic activity at `go/orchestrator/activities/spec_issue.go` per FR-002 + FR-006 + FR-007. Input `{repo, spec_ref, template_id, params}`. Resolves the issue number, runs the FR-007 idempotency check (chain query for prior `spec_issue_commented` events matching this `spec_ref` + `template_id`), skips with `spec_issue_comment_skipped` emit if found, else renders the template (from a fixed closed set per FR-006) and posts via `gh issue comment <N> --body <rendered>`
- [x] T003 [US3] Implement the `UpdateSpecIssueBody` deterministic activity at `go/orchestrator/activities/spec_issue.go` per FR-003. Reads the current body via `gh issue view <N> --json body`, applies patches by replacing named anchor blocks (`<!-- chitin:impl_pr -->...<!-- /chitin:impl_pr -->`) with the patched value, posts the updated body via `gh issue edit <N> --body <rendered>`. Unknown anchor names are no-ops, NOT errors (forward compatibility with future template additions)
- [x] T004 [US4] Implement the `CloseSpecIssue` deterministic activity at `go/orchestrator/activities/spec_issue.go` per FR-004. Posts the final `impl_pr_merged` comment via `CommentSpecIssue` (T002) for consistent template handling, then calls `gh issue close <N>`. Emits `spec_issue_closed` on success
- [x] T005 [US1] Wire `EnsureSpecIssue` into `factory_listen.go:handlePR` per FR-005. When the incoming event is `pull_request.closed` AND `merged: true` AND `isSpecPR(prNumber)` returns true (spec 115 T001's discriminator), call `EnsureSpecIssue` as a fire-and-forget goroutine — webhook returns 200 immediately, the activity runs asynchronously. Failure does NOT propagate to the webhook response per FR-008 + FR-011
- [x] T006 [US2] Wire `CommentSpecIssue(dispatch_triggered)` into the dispatch path at `cmd/chitin-orchestrator/factory_listen.go:handlePush` per US2 + FR-010. After `runSchedule` returns success, kick off a fire-and-forget `CommentSpecIssue` call with the run-ID + driver + capability params. Same async + non-blocking contract as T005
- [x] T007 [US3] Wire `CommentSpecIssue(impl_pr_opened)` + `UpdateSpecIssueBody(impl_pr: <url>)` into `handlePR` per US3 + FR-010. When the incoming event is `pull_request.opened` AND `head.ref` starts with `chitin/wu/wu-<spec-ref>` (matching the factory's branch convention), extract the spec_ref and call both activities. Async + non-blocking
- [x] T008 [US4] Wire `CloseSpecIssue` into `handlePR` per US4 + FR-010. When the incoming event is `pull_request.closed` AND `merged: true` AND `head.ref` starts with `chitin/wu/wu-<spec-ref>`, extract the spec_ref and call `CloseSpecIssue` with the final-summary params (spec_pr_url, impl_pr_url, merge_sha, elapsed wall-clock). Async + non-blocking
- [x] T009 [US5] Wire `CommentSpecIssue(dispatch_failed)` into the `factory_dispatch_failed` emit path per US5. When the dispatch fails (spec 118's failure taxonomy), the failure event ALSO triggers the spec-issue comment. The issue STAYS open per US5 — failed dispatch pauses the lifecycle, doesn't close it
- [x] T010 [US6] Implement chain emit for the five event types per FR-008. Uses the existing kernel-emit shape (the same pattern spec 121's `KernelEventSink` uses). Honors `CHITIN_DISABLE_CHAIN_EMIT=1` and `CHITIN_SPEC_ISSUE_DISABLED=1` per FR-011
- [x] T011 [US6] Implement the break-glass `CHITIN_SPEC_ISSUE_DISABLED=1` env check per FR-011. The check lives at the top of each of the four activities (T001-T004); when set, returns immediately with a log line + a `spec_issue_update_failed` event whose `op` field is `disabled_by_env`. The rest of the workflow continues unaffected
- [x] T012 [P] [US1] Hermetic test in `activities/spec_issue_test.go` — `TestEnsureSpecIssue_CreatesWhenAbsent`. Stub `gh issue list` to return empty; stub `gh issue create` to return a fake issue number. Assert the activity returns the new number with `was_new: true`, emits `spec_issue_opened`, and the rendered title + body match the template
- [x] T013 [P] [US1] `TestEnsureSpecIssue_ReusesWhenPresent` — stub `gh issue list` to return one existing issue. Assert the activity returns that issue's number with `was_new: false`, emits `spec_issue_opened` with `was_new: false` payload flag, and does NOT call `gh issue create`
- [x] T014 [P] [US2] `TestCommentSpecIssue_IdempotentOnRetry` per FR-007 — fire the activity twice with the same `spec_ref` + `template_id`. First call emits `spec_issue_commented`, posts via `gh issue comment`. Second call detects the prior event via chain lookup, emits `spec_issue_comment_skipped`, does NOT post a duplicate. The chain stub captures both events for assertion
- [x] T015 [P] [US3] `TestUpdateSpecIssueBody_PatchesNamedAnchors` — fixture body has `<!-- chitin:impl_pr --> placeholder <!-- /chitin:impl_pr -->`. Patch `{impl_pr: "#1135"}`. Assert the resulting body replaces only the anchor block; unknown patch keys (`{nonexistent: "x"}`) are silently no-ops, NOT errors
- [x] T016 [P] [US4] `TestCloseSpecIssue_PostsFinalCommentThenCloses` — stub the gh calls; assert the activity posts the `impl_pr_merged` template comment FIRST, then calls `gh issue close` SECOND, then emits `spec_issue_closed`. Order matters for human readability
- [x] T017 [P] [US5] `TestCommentSpecIssue_DispatchFailed_KeepsIssueOpen` per US5 — fire `CommentSpecIssue(dispatch_failed)` after an existing open issue. Assert the comment posts; assert NO `gh issue close` call is made; assert the issue's state remains open in the stub
- [x] T018 [P] [US6] `TestSpecIssue_GracefulOnGHFailure` per FR-008 + US6 — inject a failing `gh` shell-out (non-zero exit + stderr). Assert (a) `spec_issue_update_failed` event fires with the `op` + `stderr_tail` populated; (b) the activity returns nil-error from the caller's perspective (failure is logged + audited, NOT propagated to the calling workflow)
- [x] T019 [P] [US6] `TestSpecIssue_BreakGlassEnvDisablesAll` per FR-011 — set `CHITIN_SPEC_ISSUE_DISABLED=1`, call each of the four activities. Assert each returns immediately with `spec_issue_update_failed` emit whose `op` is `disabled_by_env`; no `gh` calls are made
- [x] T020 [US1] HTTP-route test in `factory_listen_pr_test.go` — synthesize a `pull_request.closed` `merged: true` webhook for a spec-only PR (per `isSpecPR`). Assert the handler kicks off `EnsureSpecIssue` asynchronously and returns 200 before the activity completes. Use a synchronization stub to confirm both that the activity ran AND that it didn't block the HTTP response
- [x] T021 [US1] Operator runbook `docs/runbooks/spec-126-spec-issue-for-visibility.md` — what the spec issue contains, how the lifecycle phases map to issue comments, how to find a spec's issue (`gh issue list --label chitin/spec --search <spec_ref>`), the break-glass `CHITIN_SPEC_ISSUE_DISABLED=1`, the dedup behaviour on duplicate dispatches (per spec 125), how to manually reconcile when API failures occurred, and the "this spec shipped" convention (closed `chitin/spec` issue). Cross-link to spec 098 (the webhook), spec 099 (the PR routes), spec 119 (whole-spec preservation), spec 125 (the dispatch reliability dependency)
- [ ] T022 [US1] Deployment replay validation: after the binary is deployed, observe the next spec-PR merge from the queue (likely #1140 = spec 125 itself). Assert (a) within 60s a new issue labeled `chitin/spec` exists for spec 125; (b) on dispatch, the `dispatch_triggered` comment lands; (c) on impl PR open, the `impl_pr_opened` comment + body patch lands; (d) on impl PR merge, the issue closes with the `impl_pr_merged` final comment. End-to-end SC-001 + SC-002 proof
