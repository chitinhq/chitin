---
description: "Task list — 119 whole-spec single-driver dispatch"
---

- [ ] T001 [US1] Add the `--whole-spec` / `--per-task` mutually-exclusive mode flag to `chitin-orchestrator schedule` at `go/orchestrator/cmd/chitin-orchestrator/schedule.go` per FR-001. Default to `--whole-spec` when neither is specified. The flag parser MUST reject simultaneous use of both with a clear error message
- [ ] T002 [US1] Add the new `Capability` constant `CodeSpecImplement` (or analog) to `go/orchestrator/driver/capabilities.go` (or wherever capability tags live) per FR-004. The capability string value MUST be `"code.spec-implement"` so it sorts adjacent to existing `code.*` capabilities
- [ ] T003 [US1] Update the claudecode driver's `CapabilityCard` (in `go/orchestrator/driver/claudecode/`) to declare `CodeSpecImplement` when its underlying model is opus-4.7 (Tier T4). The codex driver's CapabilityCard declares the same when its model is gpt-5.5-codex (also T4) — both updates per FR-004
- [ ] T004 [US1] Implement the single-node DAG compilation path for whole-spec mode (FR-002) in `go/orchestrator/adapter/speckit/` (the speckit adapter that today produces multi-node DAGs from `tasks.md`). When the mode is `whole-spec`, the compiler returns ONE node whose ID is the spec ref, no derived edges
- [ ] T005 [US1] Implement the whole-spec context-payload builder (FR-003) — reads `spec.md`, `tasks.md`, `plan.md` from the spec dir and produces a single `WorkUnit.Context` JSON document with `{spec_md, tasks_md, plan_md, unchecked_task_ids}`. The builder is pure; tests inject fixture dirs
- [ ] T006 [US1] Extend the work-unit activity timeout per FR-005 — accept a per-work-unit `Deadline` override on the WorkUnit struct (if not already present) and honor it in `RunWorkUnitWorkflow`. Default for whole-spec dispatches: 4 hours. Operators override via `--timeout` flag on schedule
- [ ] T007 [US1] Emit the new `whole_spec_dispatched` chain event (FR-006) at the start of whole-spec invocation with payload `{spec_ref, driver_id, task_count}`. Emit `whole_spec_completed` on activity success or `whole_spec_failed` on activity error. The `scheduler_started` event remains for the (legacy) per-task path
- [ ] T008 [US2] Update the schedule subcommand's default-mode resolution to return `whole-spec` when neither flag is specified (FR-001). The change is one line; the test is the value of having an explicit test asserting it (otherwise a future refactor flips the default unnoticed)
- [ ] T009 [US2] Add the deprecation note printer for `--per-task` per FR-009 — written to stderr, NOT an error exit. The note text: "note: --per-task is the legacy fragmented-dispatch mode; --whole-spec (the new default) produces one PR per spec via a T4 driver. See spec 119 for the rationale."
- [ ] T010 [US3] Extend the `scheduler_started` chain event payload to carry a `mode` field per FR-010, with closed values `whole-spec` | `per-task`. Existing chain consumers MUST tolerate the new field without schema changes — verified by a regression test against spec 114's queue scanner and spec 118's silent-drop detector (when they ship)
- [ ] T011 [P] [US1] Hermetic test in `cmd/chitin-orchestrator/schedule_test.go` — table-driven coverage of: (a) no flag → mode `whole-spec`; (b) `--whole-spec` alone → mode `whole-spec`; (c) `--per-task` alone → mode `per-task` + deprecation note on stderr; (d) both flags → user error with named message
- [ ] T012 [P] [US1] Hermetic test in `adapter/speckit/dag_test.go` — given a multi-task tasks.md, the whole-spec compiler returns a single-node DAG (one WorkUnit, no edges, ID == spec ref). The per-task compiler still returns the multi-node DAG for backwards-compat
- [ ] T013 [P] [US1] Hermetic test for the context-payload builder — given a fixture spec dir with spec.md/tasks.md/plan.md, the returned JSON envelope matches FR-003 exactly. Missing plan.md is tolerated (empty field). Empty tasks.md → empty `unchecked_task_ids` slice
- [ ] T014 [P] [US3] Hermetic test for the chain-event emission — schedule a whole-spec dispatch, assert exactly one `whole_spec_dispatched` event fires with the expected payload shape. Same for `whole_spec_completed` on success path
- [ ] T015 [US1] Operator runbook `docs/runbooks/spec-119-whole-spec-dispatch.md` — when to use `--whole-spec` (default; new specs), when to use `--per-task` (genuine cross-cutting refactors; cross-project work), how to override the timeout, how to read the new chain events. Also notes the FR-007 + FR-008 preservation contract: spec 113's iteration loop and spec 094 / 116's dialectic + re-review fire on the resulting PR identically to per-task — operators see no change in the review pipeline
- [ ] T016 [US1] Regression test verifying FR-007 + FR-008 invariants: stage a synthetic PR produced by whole-spec dispatch, fire a Copilot review webhook, assert spec 113's PRIterationWorkflow dispatches with the same handler path as the per-task baseline. Same fixture, also asserts the spec 094 reviewer-selection activity reads the PR identically regardless of dispatch mode
- [ ] T017 [US3] One-shot measurement script `scripts/spec-119-mode-comparison.py` — walks the chain for the last N days, groups by `mode`, computes median wall-clock-to-PR and operator-touch-time per mode. Used for SC-002 / SC-003 / SC-006 measurement during early rollout
- [ ] T018 [US1] Dispatch THIS spec (119) under whole-spec mode as the canonical first proof — the spec ships its own demonstration. After T001-T016 land, run `chitin-orchestrator schedule 119-whole-spec-dispatch --whole-spec` against a stubbed driver in a test environment; assert one PR materializes and addresses every task. This is the meta-validation step
