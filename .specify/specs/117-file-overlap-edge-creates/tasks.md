---
description: "Task list — 117 file-overlap edge inference for shared support files"
---

- [x] T001 [P] [US1] Add the EdgeReason constant `same_directory_overlap` (FR-002) alongside the existing `file_overlap` constant in `go/orchestrator/adapter/speckit/edges.go`. The constant value MUST be the string `"same_directory_overlap"` so downstream chain emission and CLI output render the kind verbatim
- [x] T002 [P] [US1] Implement the directory-set extractor `fileDirectories(tasks []Task) map[string]map[string]struct{}` (FR-003) in `go/orchestrator/adapter/speckit/edges.go` — mirrors `fileScopes()` but applies `filepath.Dir` to each extracted path and sets the value set to the resulting directories per task id. Use `filepath.Clean` first so `./pkg/x.go` and `pkg/x.go` normalise to the same parent. Paths with no directory component resolve to `"."`. FR-005 (a task naming files in multiple directories contributes to every directory's set) is satisfied by the per-task value-set shape
- [x] T003 [US1] Extend `deriveFileOverlapEdges` (and its in-loop variant `fileOverlapEdgesAt`) to emit the new edge per FR-001: for each parallel-task pair where both have non-empty file scopes, the basename scopes do NOT overlap, AND the directory sets DO overlap, append a `derivedEdge{Reason: EdgeReasonSameDirectoryOverlap}`. The existing `file_overlap` edge MUST win when both apply (FR-006 precedence)
- [x] T004 [P] [US1] Hermetic test fixture in `go/orchestrator/adapter/speckit/edges_test.go` — table-driven cases covering: (a) two parallel tasks naming different files in the same dir → new edge; (b) two parallel tasks naming different files in different dirs → no edge; (c) two parallel tasks naming the same file → existing `file_overlap` edge wins per FR-006; (d) sequential tasks → no edge regardless of dirs; (e) tasks with empty scope → no edge; (f) paths with `./` prefix vs without → same dir per FR-003; (g) one task naming multiple dirs per FR-005 → overlaps with any task naming a file in any of those dirs; (h) a task that names a directory but no file is NOT serialised against any other task per FR-004 (out-of-scope; the extractor leaves it absent from the directory set)
- [x] T005 [P] [US1] Test the `same_directory_overlap` edge reason value renders verbatim in `derivedEdge.Reason` so chain emission and CLI output see the canonical string — assert against the literal `"same_directory_overlap"` per FR-002, no enum wrapping
- [x] T006 [P] [US2] Add a test fixture mirroring the spec 114 scenario: three parallel tasks each naming a different file in `internal/queue/` (`format_table.go`, `format_md.go`, `format_json.go`). Assert that the inference now serialises T01→T02→T03 (each subsequent task depends on every prior with the same directory), and that EACH edge carries `Reason: "same_directory_overlap"`
- [x] T007 [P] [US2] Add a test fixture mirroring the spec 115 scenario: two parallel tasks each naming a different file in `internal/speclint/` (`l03_task_fr.go`, `l04_events.go`). Assert the second task depends on the first with the new reason kind
- [x] T008 [US1] Hermetic regression test in `edges_test.go` (FR-007 backwards-compat): load each shipped spec from `.specify/specs/089-*/tasks.md` through `.specify/specs/116-*/tasks.md` (glob), run `DeriveEdges`, and assert the inference produces a SUPERSET of the prior edges. The test SKIPS any spec whose `tasks.md` doesn't parse rather than failing
- [x] T009 [US1] Documentation update in `go/orchestrator/adapter/speckit/edges.go`'s package-level comment — add a paragraph naming the two file-overlap rules and the FR-006 precedence (same-basename wins over same-directory). The spec 112 US1 section stays; the new paragraph extends it
