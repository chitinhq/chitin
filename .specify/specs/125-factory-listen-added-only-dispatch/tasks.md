---
description: "Task list — 125 factory-listen Added-only dispatch (stop spurious re-dispatch on impl-PR merge)"
---

- [ ] T001 [US1] Update the dispatch loop in `cmd/chitin-orchestrator/factory_listen.go:extractSpecRefs` (called by `handlePush`) per FR-001 + FR-004 + FR-005. Replace the union iterator `append(append([]string{}, c.Added...), c.Modified...)` with iterating `c.Added` only. The change is one line; the rest of the spec-ref collection logic (the `specPathPattern.FindStringSubmatch` match + `seen[m[1]] = struct{}{}` insert) is unchanged — FR-004 preserves byte-identical behaviour on the Added path. FR-005 enforces no new config: no flag, no env var, no config-file changes touched by this task
- [ ] T002 [US4] Add a SECOND loop that iterates `c.Modified` ONLY for the chain-emit side per FR-002 + FR-007: for each path that matches `specPathPattern` and is NOT also in `c.Added` (the FR-003 dedup), emit one `factory_dispatch_filtered` chain event with payload `{spec_ref, commit_sha, path, reason: "modify_only"}` — the `reason` value MUST be drawn from the canonical taxonomy declared by FR-007. Emit follows the existing kernel-emit pattern (the same shape spec 121 uses at `internal/blob/fsstore.go:KernelEventSink`)
- [ ] T003 [US3] Implement the Added/Modified dedup per FR-003 — build a set of the Added paths first, then when iterating Modified for the FR-002 emit, skip paths already in the Added set. The dedup is per-path within a single commit (a path in both arrays of the same commit means Added wins)
- [ ] T004 [P] [US1] HTTP-route test in `cmd/chitin-orchestrator/factory_listen_test.go` — `TestHandlePush_ModifyOnlyDoesNotDispatch`. Synthesize a push payload mirroring the 2026-05-26 #1138 trigger: ref=`refs/heads/main`, one commit with `tasks.md` path in `Modified` only. Assert response is `{dispatched: false, spec_refs: []}` and zero workflows started. Stub `runSchedule` records call count
- [ ] T005 [P] [US2] HTTP-route test — `TestHandlePush_AddedTriggersDispatch`. Synthesize a payload where `tasks.md` is in `Added`. Assert exactly one workflow starts with the matching spec ref. This is the spec-119-happy-path regression guard
- [ ] T006 [P] [US3] HTTP-route test — `TestHandlePush_AddedPlusModifiedFiresOnce`. Synthesize a payload with the same path in BOTH arrays. Assert exactly one workflow starts (not two); assert NO `factory_dispatch_filtered` event fires (Added wins, the Modified entry is silent dedup)
- [ ] T007 [P] [US4] HTTP-route test — `TestHandlePush_ModifyOnlyEmitsFilteredEvent`. Same fixture as T004; assert exactly ONE `factory_dispatch_filtered` event fires with the expected payload shape (`spec_ref` + `commit_sha` + `path` + `reason: "modify_only"`). The chain emit is captured via the standard `CHITIN_KERNEL_BIN` interception pattern other tests use
- [ ] T008 [US1] Replay test — `TestHandlePush_Replays1138Trigger`. Pin the exact webhook payload that produced #1138 (commit on `main` with `.specify/specs/121-driver-output-blob-store/tasks.md` in `Modified`, no `Added` entries for that path). Assert under the fix: no dispatch, exactly one `factory_dispatch_filtered` event. Without the fix (e.g. a future regression), the test asserts dispatch fires — the test is the canonical regression guard
- [ ] T009 [US1] Operator runbook `docs/runbooks/spec-125-factory-listen-added-only-dispatch.md` per FR-006 — explains the Added/Modified distinction, the 2026-05-26 motivating evidence (link to obs S1799 + the #1132 #1133 #1138 close comments), the manual re-dispatch path (`chitin-orchestrator schedule <spec-ref>` per FR-006), how to query the chain for `factory_dispatch_filtered` events to audit what was filtered, and the design rationale (Added is the contract; Modified is impl-driver-side noise). Cross-link to spec 098 (the factory), spec 119 (whole-spec dispatch the trigger protects), spec 123 (auto-merge, which depends on this fix for correctness)
- [ ] T010 [US1] Deployment replay validation: after the binary is deployed, observe the next spec-PR merge in the queue (#1136 / #1137 / #1139). Assert (a) the spec dispatches exactly once via the Added path; (b) when its impl PR subsequently merges, NO duplicate is opened; (c) the chain shows the `factory_dispatch_filtered` event for the impl-PR merge's Modify entry. This is the SC-001 + SC-002 proof, end-to-end
