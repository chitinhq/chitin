---
description: "Task list — 118 closed reason taxonomy for factory_dispatch_failed + work-unit silent-drop visibility"
---

- [x] T001 [P] [US1] Define the `FactoryDispatchFailureKind` closed taxonomy as a typed string in `go/orchestrator/cmd/chitin-orchestrator/factory_dispatch_failure_kind.go` with constants for each FR-001 kind. Include a `Valid()` predicate that returns true for declared values, false for any other input
- [x] T002 [P] [US1] Implement `classifyDispatchError(err error) FactoryDispatchFailureKind` (FR-003) in the same file — a pure helper that walks the error via `errors.As` (preferred) and falls back to `errors.Is` / substring match for legacy string-typed errors from the underlying packages. Returns `FactoryDispatchFailureKindInternal` for unrecognised errors. The classifier is the only place new heuristics get added per FR-007 (taxonomy growth contract — drivers MUST NOT invent reasons)
- [x] T003 [US1] Extend `emitFailureEvent` at `go/orchestrator/cmd/chitin-orchestrator/factory_listen.go:622` (FR-002) to accept a `failure_kind FactoryDispatchFailureKind` parameter and write it into the payload as a top-level `failure_kind` field. Keep the existing `error` field present so legacy grep workflows continue to function
- [x] T004 [US1] Update the one call site of `emitFailureEvent` at `factory_listen.go:448` to classify the error via `classifyDispatchError` before invoking — closes the FR-002 wiring
- [x] T005 [P] [US1] Hermetic test in `factory_listen_test.go` — table-driven cases that synthesize each FR-001 failure kind, call `classifyDispatchError`, and assert the returned kind. One row per declared kind plus a row for an unrecognised error asserting the `internal` fallback. Includes an FR-007 negative test: a fabricated "novel" error string still classifies as `internal`, never as a freshly-invented value
- [x] T006 [P] [US1] Hermetic test that triggers each failure path through `handlePush` (mock the dependencies that produce the error) and asserts the emitted `factory_dispatch_failed` event payload carries the expected `failure_kind` plus the original `error` field
- [x] T007 [P] [US2] Define the new chain event_type constant `work_unit_completed_without_deliverable` in `go/orchestrator/activities/deliver.go` (or a sibling shared constants file). Define the payload shape struct `WorkUnitCompletedWithoutDeliverablePayload` with the FR-004 fields and json tags matching the spec wording verbatim
- [x] T008 [US2] Extend `DeliverWorkProduct.Execute` at `go/orchestrator/activities/deliver.go` (FR-005) to emit one `work_unit_completed_without_deliverable` chain event on each path that returns nominal success without a delivered PR — the existing no-changes / push-failed / open-PR-failed branches. The emit MUST happen INSIDE the activity body so the chain event survives the activity's nominal success exit; the activity's `Result.Explanation` MUST also name the missing deliverable per FR-005
- [x] T009 [P] [US2] Hermetic test in `activities/deliver_test.go` — for each FR-004 reason kind, run a `DeliverWorkProduct.Execute` invocation that triggers that branch (stubbing git/gh), assert one chain event fires with the correct reason and the activity result still reports nominal success
- [x] T010 [P] [US3] Extend the reason taxonomy in `go/orchestrator/internal/queue/reason.go` (FR-006) — add `silent_drop` to the `fr008ReasonTaxonomy` slice (chain-derived section). `ValidateReasonKind` automatically picks up the new value via the existing map-init pattern. Spec 114's reason taxonomy is the originating set this spec extends
- [x] T011 [P] [US3] Extend the scanner in `go/orchestrator/internal/queue/scan.go` — add `work_unit_completed_without_deliverable` to the `escalationEventTypes` map and update `classifyReason` to return `"silent_drop"` for that event_type. Mirror the spec 112 US2 `sibling_rebase_failed` precedent where the event_type IS the reason
- [x] T012 [P] [US3] Extend the filter in `go/orchestrator/internal/queue/filter.go` — `silent_drop` events join to PRs the same way other chain-derived reasons do. For silent drops that have NO associated PR (the deliverable that didn't get created is a PR), the entry surfaces with the spec_ref and task_id from the payload as the row identity. This requires extending `Entry` with optional `TaskID` + `SpecRef` fields that take precedence over `PRNumber` when present
- [x] T013 [P] [US3] Hermetic test in `internal/queue/filter_test.go` covering: (a) a `silent_drop` event with a PR number → entry has that PR; (b) a `silent_drop` event with no PR (task never opened one) → entry has spec_ref + task_id and empty PRNumber. Existing reason-kind tests remain green
- [x] T014 [US1] Add a one-shot replay script `scripts/spec-118-reclassify.py` that walks `~/.chitin/events-*.jsonl`, applies the new classifier to every `factory_dispatch_failed` event from the last 7 days, and prints the kind histogram. Used for SC-001 measurement
- [x] T015 [P] [US2] Operator-runbook update at `docs/runbooks/spec-118-dispatcher-visibility.md` — what each FR-001 failure kind means, how to grep the chain for one class, how to triage a `silent_drop` entry surfaced by the queue
