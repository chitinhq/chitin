---
description: "Task list — 122 report freshness canary + stale-badge contract"
---

- [x] T001 [US1] Implement the pure-function freshness checker at `go/orchestrator/internal/reportfreshness/check.go` per FR-001 + FR-003. Signature: `Check(ctx, paths []WatchedPath, now time.Time) (Result, error)`. Resolves each path's effective `generated_at` via embedded metadata → mtime → missing. No I/O beyond `os.Stat` + file open + the regex parse; no time.Now in this package
- [x] T002 [US3] Implement the embedded-metadata parser at `go/orchestrator/internal/reportfreshness/metadata.go` per FR-005. Regex `<!--\s*chitin-report-meta:\s*({[^}]*})\s*-->` case-insensitive across the first 4 KiB of the file. JSON-parse the captured group; parse-failure returns `(nil, ErrMetadataUnparseable)` and the caller falls back to mtime per FR-003
- [x] T003 [US1] Implement the watched-paths config loader at `go/orchestrator/internal/reportfreshness/config.go` per FR-002. YAML schema `{paths: [{path, sla_hours}], cadence_minutes, escalation_cooldown_hours}`. Default cadence 360 (6h), default escalation cooldown 24h. Missing config file → emit a typed `ErrConfigMissing` so the activity wrapper can handle gracefully per the FR-002 edge case
- [x] T004 [US1] Bundle the default config at `go/orchestrator/internal/reportfreshness/default-config.yaml` per FR-002. Initial paths (all absolute): `/home/red/labs/local-ai-lab/wiki/assets/chain-summary-latest.html`, `/home/red/labs/local-ai-lab/wiki/assets/board-audit-latest.html`, `/home/red/labs/local-ai-lab/wiki/assets/industry-scan-latest.html`, each `sla_hours: 24`. The default config is the spec's commitment that these three paths get watched out of the box — the operator can override but the bundled defaults solve the 2026-05-26 evidence
- [x] T005 [US1] Implement the `CheckReportFreshness` activity wrapper at `go/orchestrator/activities/report_freshness.go` per FR-001. Wraps the pure checker (T001), reads the config (T003), emits the FR-004 chain events. Idempotent + side-effect free apart from chain emit per FR-011. The activity is what the schedule (T010) invokes and what the CLI subcommand (T007) shares via the pure-function core
- [x] T006 [US1] Implement the closed-taxonomy chain event emit at `go/orchestrator/activities/report_freshness.go` per FR-004. Five event types: `stale_report_detected`, `report_fresh`, `report_missing`, `stale_report_escalated`, `stale_report_suppressed`. Payload schemas match the spec.md data-model block exactly. Honors `CHITIN_DISABLE_CHAIN_EMIT=1` for tests
- [x] T007 [US5] Implement the `chitin-orchestrator reports check` subcommand at `go/orchestrator/cmd/chitin-orchestrator/reports.go` per FR-009. Flag `--config <path>`. Calls the FR-001 pure function; renders a fixed-column table; exit codes 0/2/3 per fresh/stale/missing. Shares code with the activity via the pure-function core — no duplication
- [x] T008 [US5] Implement the `chitin-orchestrator reports list` subcommand at `go/orchestrator/cmd/chitin-orchestrator/reports.go` per FR-010. Read-only echo of the configured watched paths. Exit code 0 always; 1 only on config parse error
- [x] T009 [US4] Implement the rate-limiter at `go/orchestrator/activities/report_freshness.go` per FR-007. Query the chain for prior `stale_report_escalated` events on the same path within `escalation_cooldown_hours`. If any exists, emit `stale_report_suppressed` INSTEAD of escalating; payload includes `cooldown_remaining_hours` + `prior_escalation_at`
- [x] T010 [US1] Register the canary as a scheduled job at `go/orchestrator/schedules/report_freshness_schedule.go` per FR-006 + FR-012. Cadence default 6h; configurable via the watched-paths config. The schedule MUST appear in `chitin-orchestrator schedules list` output so operators can confirm "the canary is running" without reading the chain
- [x] T011 [US4] Wire `stale_report_detected` → `stale_report_escalated` via the existing `DiscordNotify` activity per FR-008 (introduced by spec 080, reused by spec 114 FR-009 — `go/orchestrator/activities/notify.go`). The chain reason kind on the escalation event is `stale_report` (new closed-taxonomy value — see T012). Message body includes path + age + SLA + age_source + `file://` URL. Returned message ID lands in the escalation event's `notify_message_id` field
- [x] T012 [US4] Extend spec 114's reason taxonomy with `stale_report` per FR-008. The change touches `internal/queue/reason.go` (the `fr008ReasonTaxonomy` slice / `fr008ReasonSet` map that `ValidateReasonKind` uses) and any spec-118-style validator that asserts closed-set membership. This is the only cross-spec change; keep it minimal — one new closed value, no behavior change to existing kinds
- [x] T013 [P] [US1] Hermetic test in `internal/reportfreshness/check_test.go` — table-driven: (a) fresh file (mtime 1h ago, SLA 24h) → not stale; (b) stale file (mtime 100h ago, SLA 24h) → stale with `age_source: "mtime"`; (c) missing file → missing; (d) directory at path → missing; (e) embedded metadata fresh + mtime stale → not stale, `age_source: "embedded"`; (f) embedded metadata stale + mtime fresh → stale, `age_source: "embedded"` (producer-lying case)
- [x] T014 [P] [US3] Hermetic test in `internal/reportfreshness/metadata_test.go` — (a) valid metadata block parses correctly; (b) malformed JSON inside the block returns `ErrMetadataUnparseable`; (c) metadata block at byte offset > 4 KiB is NOT found (the regex windows to first 4 KiB); (d) multiple metadata blocks — first wins, doc this as the contract
- [x] T015 [P] [US1] Hermetic test in `activities/report_freshness_test.go` — full activity round-trip against a fixture dir with one fresh + one stale + one missing report. Assert exactly the right set of chain events fire with the right payload shape. Use `CHITIN_DISABLE_CHAIN_EMIT=0` and an in-memory chain sink
- [x] T016 [P] [US4] Hermetic test for the rate-limiter (T009) — fire 5 detections against the same path within 1h of simulated wall-clock. Assert: 1 `stale_report_escalated` + 4 `stale_report_suppressed` events, each suppressed event carrying decreasing `cooldown_remaining_hours`
- [x] T017 [P] [US5] Hermetic test in `cmd/chitin-orchestrator/reports_test.go` — `reports check` against the fixture: stdout matches the expected table format, stderr empty, exit code 2. `reports list` against the same config: stdout lists the configured paths, exit 0
- [x] T018 [US1] Integration test that the schedule registration (T010) appears in `chitin-orchestrator schedules list` output — a regression net for the FR-012 visibility invariant
- [x] T019 [US1] Operator runbook `docs/runbooks/spec-122-report-freshness-canary.md` — what the canary watches by default, how to add a path, how to tune the cadence + cooldown, what a `stale_report` Discord notification looks like, the producer opt-in contract for embedded metadata (the HTML comment block schema), how to silence a known-broken report temporarily. Cross-link to spec 114 (the escalation surface) and the 2026-05-26 Ares scan that triggered this spec
- [x] T020 [US1] Initial deployment: commit the bundled default-config.yaml (T004) AND ensure the live `~/.chitin/report-freshness.yaml` is installed when the operator runs the spec's installer step (documented in T019). The canary watching ONLY the bundled defaults is the SC-001 proof for the 2026-05-26 evidence — three paths flagged, three Discord escalations, dedup per FR-007
- [ ] T021 [US1] Replay validation: after deployment, wait one cadence cycle (≤ 6h) and assert: (a) at least one chain event fired for each of the three Ares-flagged paths (the canary saw them); (b) each routed to exactly one Discord notification (the rate-limiter held); (c) the chain shows `stale_report_escalated` events with correct `notify_message_id` values. This is the canonical end-to-end demonstration of US1 + US4
