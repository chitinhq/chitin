---
description: "Task list — 120 claudecode-glm driver"
---

- [x] T001 [P] [US1] Define the `TierLocal` constant (FR-007) in `go/orchestrator/driver/taxonomy.go` alongside the existing `TierFrontier`. Add `CostZero` constant if not already present. Both constants are sortable string-typed enums so future cost-aware selectors can express preference rules
- [x] T002 [P] [US1] Implement the `claudecodeglm` driver package at `go/orchestrator/driver/claudecodeglm/driver.go` per FR-001 + FR-002 — the `AgentDriver` interface, `Card()` returning the FR-002 shape with `CapCodeImplement` + `CapSpecImplement`, `Tier: TierLocal`, `CostClass: CostZero`. Shells out to `ollama launch claude` per FR-001
- [x] T003 [US1] Implement `Ready()` per FR-003 — verify `ollama` binary on PATH, `GET http://localhost:11434/api/tags` returns 2xx, target model name appears in the response, AND `claude` CLI binary on PATH. 2s timeout on the daemon probe. Returns `(false, "<one-line reason>")` naming the specific failed precondition so the registry's selector + operator triage see the cause without grep
- [x] T004 [US1] Implement `Invoke()` per FR-004 — builds the command line `ollama launch claude --model <card.Model> -- <claude-print-args>`, propagates the prompt + worktree-path the same way the existing claudecode driver does, parses the same Result shape on completion. Honors `CHITIN_OLLAMA_BIN` env override (FR-005)
- [x] T005 [P] [US1] Unit tests in `go/orchestrator/driver/claudecodeglm/driver_test.go` — table-driven cases covering Card() shape, Ready() pass + each fail-reason (ollama binary missing, ollama daemon down, model not in tags, claude CLI missing), Invoke() argv assembly (mocked `ollama` echoes its argv into a captured buffer; assert FR-004 form is correct)
- [x] T006 [US1] Register `claudecodeglm.New()` in `go/orchestrator/cmd/chitin-orchestrator/main.go`'s `buildRegistry()` alongside the other drivers. The registration line is one entry in the existing slice; capability-based selection (spec 075 FR-007) takes over from there
- [x] T007 [P] [US2] Hermetic test for the Ready() fallback path — synthesise a registry with ONLY claudecode-glm, set the ollama-port to an unreachable one (via `CHITIN_OLLAMA_BIN` pointing at a shim that returns a failing health check), invoke a CapSpecImplement work unit, assert the activity returns `BlockedUnroutable` (or equivalent) without panicking. Confirms the routing layer treats Ready=false as routing-eligible-but-skip per spec 075
- [x] T008 [US1] Operator runbook at `docs/runbooks/spec-120-claudecode-glm.md` (FR-008) — prerequisites (ollama v0.21+, `ollama pull glm-5.1`, claude CLI installed), one-line smoke test (`ollama launch claude --model glm-5.1 -- -p "say hi"`), how to verify the driver registered (grep orchestrator startup log), `CHITIN_DRIVER_ALLOW` patterns for forced routing during testing, troubleshooting common failure modes
- [x] T009 [US3] Validate the FR-006 chain event payload — `scheduler_started` carries `driver_id` after selection. If the existing payload doesn't yet include it, extend with the field via the same backwards-compat pattern spec 119 used for `mode`. Test asserts the field appears on a successful whole-spec dispatch
- [x] T010 [US1] Hermetic end-to-end test (under `// +build integration` or similar) that dispatches a small fixture spec under `--whole-spec` with `CHITIN_DRIVER_ALLOW=claudecode-glm` and asserts the work-unit completes with `driver_id: "claudecode-glm"` in the chain. Requires `ollama` + `claude` + the target model in CI — gate behind a build tag so the default test suite stays hermetic
- [x] T011 [US1] SC-004 empirical measurement — dispatch spec 117 under claudecode-glm only, evaluate the resulting PR. Document findings in the runbook (T008). If glm-5.1's output is materially worse than codex's, note the recommended fallback default in the runbook; the routing infrastructure remains valid regardless
