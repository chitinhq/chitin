---
description: "Task list — 127 workflow input-validation guard (fail-fast on placeholder repos, close the test leak path)"
---

- [ ] T001 [US1] Implement the pure validator at `go/orchestrator/internal/wfvalidate/pr_review.go` per FR-001 + FR-002 + FR-010 + FR-011 (the closed reason taxonomy that `*RejectError.Reason` MUST be drawn from). Export `Validate(in PRReviewInput) error` returning typed `*RejectError{Reason, Detail}` on rejection, `nil` on accept. Closed placeholder set built once at init time: `owner/name`, `o/r`, `test/repo`, `example/example`, `foo/bar`. Structural rejections per FR-010: empty `Repo` → `missing_repo`; zero `PRNumber` → `missing_pr_number`; `Repo` not matching `^[a-zA-Z0-9._-]+/[a-zA-Z0-9._-]+$` → `malformed_repo_slug`. Pure function — no I/O, no globals beyond the init-time set
- [ ] T002 [US1] Wire the validator into `PRReviewWorkflow` at `go/orchestrator/workflows/pr_review.go` per FR-003. The call MUST be the workflow's FIRST statement, before any `workflow.ExecuteActivity`. On `*RejectError`: emit one `workflow_input_rejected` chain event (T005) then return `temporal.NewNonRetryableApplicationError(err.Error(), "InputRejected", err)`. Temporal treats non-retryable errors as terminal — no retries, no further activity dispatches
- [ ] T003 [US3] Implement `chitin-orchestrator workflows list-zombies` subcommand at `go/orchestrator/cmd/chitin-orchestrator/workflows.go` (new file) per FR-005. Iterates `temporal workflow list --query 'ExecutionStatus="Running"'`, for each fetches the start input via `workflow show`, applies the FR-002 set membership check + the `--retry-threshold` (default 50) check on pending activity attempt count. Output: fixed-column table `workflow_id | run_id | workflow_type | repo | pr_number | attempt_count | started_at`. Exit codes 0 (empty) / 2 (any rows). Reads the same Temporal host as the rest of the orchestrator (env fallback to `$TEMPORAL_HOSTPORT`)
- [ ] T004 [US4] Implement `chitin-orchestrator workflows terminate-zombies` subcommand in the same file per FR-006. Walks the same set as T003, terminates each via `client.TerminateWorkflow(ctx, workflowID, runID, reason, details)` with `--reason` flag (default `placeholder_repo_input_<YYYY-MM-DD>`). Safety belt: re-validate each workflow's input against the FR-002 set before terminating; refuse and exit with code 3 if any target is NOT a placeholder, unless `--force-real-repo` is passed (documented as DANGER). Supports `--dry-run` for safe preview. The 2026-05-26 cleanup script at `/tmp/zombie-terminate.py` is the proof-of-concept; this task codifies it as a subcommand with stronger guards
- [ ] T005 [US1] Implement the `workflow_input_rejected` chain emit helper at `go/orchestrator/workflows/pr_review.go` (or a shared `workflows/emit.go` if other workflows will reuse it per FR-009). Payload matches the FR-004 closed schema exactly: `{workflow_type, workflow_id, repo, pr_number, reason, detail}`. Honors `CHITIN_DISABLE_CHAIN_EMIT=1` for tests. Emits via the existing `KernelEventSink` pattern (spec 121 reference)
- [ ] T006 [US5] Implement the `CHITIN_FACTORY_LISTEN_NO_DISPATCH` env shutoff at `go/orchestrator/cmd/chitin-orchestrator/pr_review_dispatch.go` per FR-007. When set to `1` at the top of `dispatchPRReview`, return early with `prDispatchResult{ReviewStarted: false, FailureKind: "dispatch_disabled_via_env"}` and emit NO chain event. The boot log line in `main.go` (analogous to spec 121's `blob_inline_threshold=...`) prints the env's value at orchestrator startup so operators can audit "this is unset in prod" at a glance
- [ ] T007 [US5] Update `cmd/chitin-orchestrator/factory_listen_pr_test.go` per FR-008. Every existing test in the file MUST either (a) call `t.Setenv("CHITIN_FACTORY_LISTEN_NO_DISPATCH", "1")` in its setup, OR (b) inject a stub dialer via the existing `factoryHandler.dispatcher` seam. Pick (a) for tests that don't care about dispatch (most of the file); pick (b) for tests that DO want to assert dispatch behaviour. Document the choice at the top of the test file as a contract for future tests
- [ ] T008 [P] [US1] Hermetic test in `internal/wfvalidate/pr_review_test.go` — table-driven validator tests: (a) `repo: "owner/name"` rejects with `placeholder_repo`; (b) `repo: "o/r"` rejects; (c) `repo: ""` rejects with `missing_repo`; (d) `pr_number: 0` rejects with `missing_pr_number`; (e) `repo: "not-a-slug"` rejects with `malformed_repo_slug`; (f) `repo: "chitinhq/chitin"` accepts; (g) edge: `repo: "owner-corp/chitin"` accepts (hyphen in owner is legal); (h) edge: `repo: "owner/name-2"` accepts (real-looking name that contains the placeholder as a prefix doesn't false-match — exact-match only)
- [ ] T009 [P] [US1] Hermetic test in `workflows/pr_review_test.go` using the Temporal testsuite — start `PRReviewWorkflow` with `PRReviewInput{Repo: "owner/name", PRNumber: 100, ...}`. Assert (a) workflow completes within 1 second with status `Failed` and error type `InputRejected`; (b) exactly ONE `workflow_input_rejected` event fires with the expected payload; (c) ZERO `CapturePRSnapshot` activity tasks were scheduled (use the testsuite's activity-mock to assert no calls)
- [ ] T010 [P] [US2] Hermetic test in `workflows/pr_review_test.go` — start `PRReviewWorkflow` with a real `Repo: "chitinhq/chitin", PRNumber: 1141, ...`. Stub `CapturePRSnapshot` to return a canned snapshot. Assert (a) the workflow does NOT emit `workflow_input_rejected`; (b) `CapturePRSnapshot` was called exactly once with the real input. This is the no-regression guard for legitimate dispatches
- [ ] T011 [P] [US3] Hermetic test for `workflows list-zombies` at `cmd/chitin-orchestrator/workflows_test.go` — stub the Temporal client to return two running workflows, one with `repo: "owner/name"` and one with `repo: "chitinhq/chitin"`. Assert stdout lists only the placeholder one; exit code 2. Also test the `--retry-threshold` flag: stage one legitimate workflow with attempt 100 and the threshold at 50 — assert it's listed (the threshold catches retry-stuck-but-legitimate)
- [ ] T012 [P] [US4] Hermetic test for `workflows terminate-zombies` — stub the Temporal client to return the same two-workflow scenario as T011. (a) `--dry-run` lists the placeholder, terminates nothing; (b) without `--dry-run`, terminates the placeholder, refuses to terminate the legitimate one (exit code 3); (c) with `--force-real-repo`, terminates both. Assert via the stub that `TerminateWorkflow` was called the expected number of times
- [ ] T013 [P] [US5] Hermetic test for FR-007 — set `CHITIN_FACTORY_LISTEN_NO_DISPATCH=1` via `t.Setenv`. Call `dispatchPRReview` with a normal input. Assert `out.ReviewStarted == false`, `out.FailureKind == "dispatch_disabled_via_env"`, and that the dialer was NOT called (passed-in stub dialer asserts `t.Errorf("dialer should NOT be called")`)
- [ ] T014 [P] [US5] Hermetic test for FR-008 — run the updated `factory_listen_pr_test.go` suite. Assert each test EITHER sets the env shutoff OR injects a stub dialer (via a meta-test that scans the test file's setup and verifies one of the two patterns is in effect for every test). The meta-test is the contract guard for future tests
- [ ] T015 [US1] Register the new chain event type `workflow_input_rejected` in `docs/chain-events.md` (or wherever the chain event taxonomy is documented) per FR-004. Payload schema matches T005 exactly. Cross-link from the data-model section of this spec
- [ ] T016 [US1] Operator runbook `docs/runbooks/spec-127-workflow-input-validation-guard.md` — covers (a) what the validator catches (the FR-002 closed set + FR-010 structural rejections); (b) how to read `workflow_input_rejected` events from the chain; (c) the `workflows list-zombies` + `workflows terminate-zombies` cleanup flow with the 2026-05-26 incident as the canonical example (47h of leaked workflows, 198 terminated, ≥ 50% rate-limit reduction); (d) what production should NOT have set (`CHITIN_FACTORY_LISTEN_NO_DISPATCH` MUST be unset in prod); (e) the FR-009 contract for follow-up specs (each new dispatched workflow MUST adopt the same validator pattern). Cross-link to spec 099 + spec 094 + the obs S1829 + obs 8898 (placeholder leak detection)
- [ ] T017 [US1] Deployment validation: after the binary is deployed and the orchestrator restarts, run `chitin-orchestrator workflows list-zombies` — assert zero rows (the 2026-05-26 cleanup left a clean state). Then run the updated `factory_listen_pr_test.go` suite against the production Temporal host (with `$TEMPORAL_HOSTPORT` set); assert `workflows list-zombies` STILL returns zero rows after the test run (FR-008 closed the leak). This is the SC-001 + SC-002 + FR-008 end-to-end proof
- [ ] T018 [US1] Replay validation: 24h after deployment, query the chain for any `workflow_input_rejected` events. If present, the validator caught real (likely test-CI-driven) leak attempts — record the counts in the runbook as evidence the guard works. If zero, the leak source was already closed by FR-008; the validator is defense-in-depth (still valuable, no negative signal)
