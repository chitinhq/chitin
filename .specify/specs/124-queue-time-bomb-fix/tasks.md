---
description: "Task list — 124 queue test time-bomb fix (inject `now` so conflicting_persistent doesn't get shadowed)"
---

- [x] T001 [US1] Add the internal `runQueueWithNow(opts, now time.Time, ...)` seam at `cmd/chitin-orchestrator/queue.go` per FR-001 + FR-002. The public `runQueue` becomes a one-line wrapper that calls `runQueueWithNow(opts, time.Now().UTC(), ...)`. Every downstream read of time inside the request path (`since := now.Add(-opts.Since)` at line 67, the `queue.Build(...)` call at line 89, the formatter calls at lines 97/99) MUST flow from the injected `now` — no new `time.Now()` reads inside the helper
- [x] T002 [US2] Update `TestRunQueue_HermeticAcrossAllReasonKinds` and `TestRunQueue_ReasonFilter_NarrowsToSingleKind` at `cmd/chitin-orchestrator/queue_test.go` per FR-003 — invoke the new seam (T001) with `queueTestNow` so the fixture-anchored timestamps are compared against the same frozen `now` the fixture was authored against. The `runQueueCapture` helper extends to accept an optional `now time.Time` and threads it through to `runQueueWithNow`
- [x] T003 [P] [US1] Hermetic test in `internal/queue/filter_test.go` per FR-005 — table-driven fixture pinning `matchLiveRules`'s contract for PR #9008's state (`Mergeable: CONFLICTING, UpdatedAt: now-3h, LastAutomatedCommitAt: now-1h`, chitin-authored branch). Assert the returned rule is `conflicting_persistent`. This is the regression guard for rule-order changes independent of the time-bomb
- [x] T004 [P] [US1] Hermetic test that the public `runQueue` (the CLI entry point — no seam) preserves byte-identical behaviour against a fixture-free invocation. Exit code 0 on empty repo, expected stderr/stdout shape. This is the FR-004 guard so a future refactor of the seam can't accidentally change the operator-facing path
- [x] T005 [US3] Operator/developer runbook at `docs/runbooks/spec-124-queue-time-bomb-fix.md` — documents the time-bomb pattern (frozen fixture anchor + production `time.Now()`), the seam idiom (function-level `now` parameter with default-value wrapper), and lists candidate call sites in the repo that follow the same pattern and may need future audit (at minimum: `loop/`, `schedules/`, `activities/`). Includes a one-line checklist question for future spec authors: "Does this spec's tests use a frozen `now` while the production code reads `time.Now()` directly? If yes, add a seam."
- [ ] T006 [US1] Replay validation: after the fix lands, run `go test ./cmd/chitin-orchestrator/ -run TestRunQueue` on the operator's machine (wall-clock 2026-05-26+) and confirm a clean pass. Then advance the system clock 365 days via `faketime` (or equivalent) and re-run; confirm still passes deterministically. The same fixture data MUST yield the same classifier output regardless of wall-clock. This is the SC-001 proof. **Status (2026-05-26):** wall-clock pass confirmed in CI; advanced-clock (`faketime +365d`) replay not yet executed in this environment — tracked as follow-up before closing SC-001
