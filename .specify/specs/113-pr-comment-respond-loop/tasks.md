---
description: "Task list — 113 PR comment-respond loop"
---

- [ ] T001 [P] [US1] Extend `prPayload` in `go/orchestrator/cmd/chitin-orchestrator/pr_eligibility.go` to surface `Review.State`, `Review.Author.Login`, `Review.ID` from `pull_request_review` events; add `checkPullRequestReviewEvent(p *prPayload) prEligibility` returning Eligible iff state ∈ {COMMENTED, CHANGES_REQUESTED} AND reviewer is in the configured copilot allowlist AND head ref matches `chitin/wu/*`
- [ ] T002 [P] [US1] Implement `dispatchPRIteration` in `go/orchestrator/cmd/chitin-orchestrator/pr_iteration_dispatch.go` — given an eligible review payload, resolve the work-unit metadata (driver_id, scheduler_run_id) via the PR's `sched/run/<id>` label, then `ExecuteWorkflow(PRIterationWorkflow, ...)` with deterministic WorkflowID `iteration-pr-<N>-review-<M>` and `WORKFLOW_ID_REUSE_POLICY_REJECT_DUPLICATE`. Mirror spec 112 US2's `dispatchSiblingRebase` pattern (injectable temporal dialer, nil-stderr guard, isAlreadyStarted handling)
- [ ] T003 [P] [US1] Wire the new dispatch into `factory_listen.go` `handlePR` — when `eventType == "pull_request_review"`, route through `checkPullRequestReviewEvent` then `dispatchPRIteration`, populating new `PRIteration*` fields on `prResponse` for operator visibility
- [ ] T004 [US1] Implement `PRIterationWorkflow` in `go/orchestrator/workflows/pr_iteration.go` — typed input (PRNumber, PRBranch, TargetRepo, DriverID, ReviewID, Round, SchedulerRunID), runs `FetchReviewContext` → `BuildIterationPrompt` → `InvokeDriver:<id>` → `CommitAndPushIteration` → `PostCommentReplies` → `EmitIterationTelemetry`. MaxAttempts=1 (driver-side faults don't blindly retry)
- [ ] T005 [US1] Implement `FetchReviewContext` activity in `go/orchestrator/activities/fetch_review_context.go` — `gh api /pulls/N/reviews/M` + `/pulls/N/comments` to assemble the prompt context. Returns typed `ReviewContext { Body, LineComments []{Path, Line, Body, ID} }`
- [ ] T006 [US1] Implement `BuildIterationPrompt` (pure helper in `go/orchestrator/workflows/pr_iteration_prompt.go`) — template that injects PR diff, review context, original task description, required JSON output envelope schema. Pure function; hermetic-testable
- [ ] T007 [US1] Implement `CommitAndPushIteration` activity in `go/orchestrator/activities/iteration_commit.go` — `worktree.Manager.Checkout` (reuse spec 112 US2 path), apply driver output, commit with conventional message (`review fix: ...`), `git push --force-with-lease`. Emits `pr_iteration_failed` on lease loss
- [ ] T008 [US1] Implement `PostCommentReplies` activity in `go/orchestrator/activities/post_comment_replies.go` — for each reply in driver output, `gh api -X POST /pulls/N/comments/<id>/replies -f body=...`. Skip threads with existing chitin reply (detected by author=`chitin-orchestrator`)
- [ ] T009 [US1] Implement `EmitIterationTelemetry` activity — emits the four chain events (round_started, completed, failed, escalated) via the existing kernel-emit shell-out pattern from spec 112 US2's `emitSiblingRebaseEvent`. Honors `CHITIN_DISABLE_CHAIN_EMIT=1` for tests
- [ ] T010 [US2] Extend `spec-kit/parse.go` to read `iteration_cap:` from spec frontmatter; thread through `SchedulerWorkflow` → `WorkUnitWorkflow` → PR labels (`sched/cap/<N>`) so `PRIterationWorkflow` can read the cap without re-reading the spec
- [ ] T011 [US3] In `checkPullRequestReviewEvent`, detect a human reviewer (login NOT in the copilot allowlist) and short-circuit to emit `pr_iteration_escalated { reason: "human_reviewer_present" }` without dispatching the workflow
- [ ] T012 [US1] Add hermetic test in `go/orchestrator/workflows/pr_iteration_test.go` — Temporal testsuite env, stub driver returns a known fixup, assert workflow result has correct round count, action_counts, and emitted events
- [ ] T013 [US1] Add hermetic test in `go/orchestrator/cmd/chitin-orchestrator/pr_iteration_dispatch_test.go` — fake gh + fake temporal dialer, assert deterministic WorkflowID and AlreadyStarted dedup (mirror spec 112 US2's pattern)
- [ ] T014 [US1] Add HTTP-route test in `factory_listen_pr_test.go` — `pull_request_review.submitted` event on chitin/wu/* branch with copilot reviewer triggers dispatch; same event with human reviewer escalates instead
- [ ] T015 [US1] Add escalation cap test — drive 3 rounds with stubbed driver returning the same comments unfixed each time; assert 3rd round emits `pr_iteration_escalated` with `reason: "cap_hit"` and applies the `chitin-escalated/comment-cap` label
- [ ] T016 [US1] Operator runbook in `docs/runbooks/spec-113-comment-respond-loop.md` — how to inspect ongoing iterations (`gh pr list --label chitin-iterating`), how to force-escalate (`gh pr edit <N> --add-label chitin-escalated/manual`), how to override the cap per spec
