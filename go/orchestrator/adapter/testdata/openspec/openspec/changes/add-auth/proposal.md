# Change: Add Authentication

## Why

The application currently has no authentication. This change adds login and
removes the legacy anonymous-access path.

## What Changes

- ADD a login requirement.
- MODIFY the session requirement.
- REMOVE the anonymous-access requirement.
