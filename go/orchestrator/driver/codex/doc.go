// Package codex will hold the Codex agent driver (spec 075, task T013) — a
// gateway/API runtime behind the AgentDriver contract.
//
// TODO(spec-075 Phase 3, US1): implement driver.go — an AgentDriver whose
// Invoke calls the Codex agent (Hermes-hosted) over its gateway/API,
// publishing a CapabilityCard and reporting readiness from the gateway's
// health and quota. Left as a documented stub by the Phase 0 foundation
// slice (T001–T011).
package codex
