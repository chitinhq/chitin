// Package hermes will hold the Hermes agent driver (spec 075, task T014) —
// a gateway/API runtime behind the AgentDriver contract.
//
// TODO(spec-075 Phase 3, US1): implement driver.go — an AgentDriver whose
// Invoke calls Hermes over its gateway/API; its CapabilityCard declares the
// web/X/browser/code capability tags Hermes brings. Left as a documented
// stub by the Phase 0 foundation slice (T001–T011).
package hermes
